import ReactDOM from 'react-dom/client';
import './index.css';
import { LicenseGeneratorApp } from './features/LicenseGeneratorApp';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <LicenseGeneratorApp />,
);
